var ApplicationRoute = Ember.Route.extend({
  setupController: function(controller, model) {
    // var cardRegistry = this.container.lookup('cardRegistry:main');
    // controller.set('authCard', );
  }
});

export ApplicationRoute;
